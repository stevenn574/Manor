<li>
	<b><%= chore.user %></b>
	<%= chore.action %> - <%= chore.points %>pts<br/>
	<span class="small"><%= time_ago_in_words(chore.created_at) %> ago | <%= link_to("delete", chore_path(chore), :method => :delete) if chore.user == current_user %></span>
</li>